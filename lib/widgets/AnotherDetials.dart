import 'package:flutter/material.dart';
class AnotherDetails extends StatelessWidget {
  const AnotherDetails({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Text("anotherDetials");
  }
}
