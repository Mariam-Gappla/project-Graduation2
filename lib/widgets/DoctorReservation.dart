import 'package:flutter/material.dart';
class DoctorReservation extends StatelessWidget {
  const DoctorReservation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Doctor Reservation");
  }
}
